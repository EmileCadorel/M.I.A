mod word::dtmnt;

import std::io;
import word::base;


/**
 * A determinant. "la", "tiu", "tio", "ties", etc.
 */
pub class @final Determinant over Word {

    pub self (radical : [c32])
        with super (radical)
    {}

    impl Printable;    
}


/**
 * A possessive. "mia", "via", "lia", etc.
 */
pub class @final Possessive over Word {
    
    pub self (radical : [c32])
        with super (radical)
    {}

    impl Printable;
}
